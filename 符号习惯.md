# 通用规则：

* 算子使用罗马字体，如 ${\rm Ker}\,f$，以与变量区分。尤其注意微积分中的 ${\rm d}x$。
* 向量或矩阵使用粗斜体，如 $\bm Av$。
* 一些类型使用空心字体或粗体，如 $\mathbb{R}$ 表示实数类型，$\mathbb{K}$ 或 $\mathbb{F}$ 表示域等。
* 一些较大的“容器”或特定算子使用 Caligraphy 字体，如拓扑 $\mathcal{T}$。
* 一些特殊算符使用 Script 字体，如 $\mathscr{F}$ 表示 Fourier 变换。

# 具体规则：

* 尽量以类型论为元语言，避免书写 $x \in \R$，而是写 $x : \R$。
* 类型集合类型写作 $S : \mathrm{Set}(\R)$。