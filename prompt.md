# Fulcrum 数学笔记写作指南

本指南面向笔记书写者及智能体，用于按准受控自然语言（CNL）风格书写数学笔记。

---

## 一、项目结构

```
Mathematics/
├── NN-TopicName/          # 编号-主题名，其中编号是 MSC 分类标准中的编号
│   ├── main.typ           # 笔记正文
│   └── export.typ         # 对外导出的概念链接（供其他笔记引用）
├── contributors.typ       # 贡献者署名
└── export.typ             # 汇总导入，供根文档使用
```

---

## 二、文档结构

### 1. 新建领域主题目录

- 目录名格式：`NN-MathFieldName`（两位数编号 + 英文领域名）
- 新建 `main.typ` 与 `export.typ`

### 2. main.typ 头部

主要需要导入三类包：
1. Fulcrum 模板包
2. 术语集 `export.typ`
3. 贡献者签名 `contributors.typ`

通过 `#show` 命令改变文档格式：
1. `#show: FulcrumCN` 改变文档格式为 FulcrumCN
2. `#show: MathFieldStyle` 来启用一些内置记号

```typ
#import "../../Fulcrum-Template-Typst/Fulcrum.typ": *
#import "../contributors.typ": *
#import "../export.typ": *

#show: FulcrumCN
#show: SetStyle          // 若涉及集合论
#show: TypeStyle         // 若涉及类型论
#show: AlgebraStyle      // 若涉及代数
// 按需启用其他 Style

#title[标题中文名]
#outline(title: "目录", indent: 2em)
#pagebreak()
```

### 3. 正文结构

- **章**：`= 章标题`
- **节**：`== 节标题`

### 4. 数学知识条目

**约定**：在章节开头用 `#约定[]` 声明本节变量语境

依序书写知识条目，规则在下方。

### 5. 维护 export.typ

所有术语通过 `optionLink` 函数声明为常量，并存入 `export.typ` 文件中。对于内置符号，用 `#show` 命令改变其默认展示模式。

```typ
#import "../../Fulcrum-Template-Typst/Fulcrum.typ": *

#let 概念A = optionLink("ConceptAUuid", [概念A中文名])
#let 概念B = optionLink("ConceptBUuid", [概念B中文名], url: "https://...")  // 默认情况下链接到维基百科
```

### 6. 更新 Mathematics/export.typ

对于新数学领域的 `export.typ`，在 `Mathematics/export.typ` 中加入对应 `#import`。

---

## 三、条目类型简介

各个数学条目命令的接口参见 `Fulcrum-Template-Typst/Fulcrum.typ` 文件。

下面介绍其用法：

*每个条目块均可以通过 `isExtension: true` 参数声明为扩展条目，不修改计数器并与上一条目衔接。*

### 公理 / 规则（黄色）

公理是无证明的定理，或者无构造的对象。公理的书写规则与定义、定理相同，但不需要给出构造或证明。

规则是推理系统中构造元语言中新的数学对象的构造子，一般只会在数理逻辑中出现。

### 定义 / 结构 / 实例（绿色）

此三者本质上均为定义，均向语境中增派一个新的数学对象。

一个结构向语境中增派一个结构体类型 / 乘积类型，其中包含多个成员类型的定义。结构可以继承自其他结构。

一个实例是一个类型为某结构体类型的对象，应给出相应结构体（包括所继承的）全体成员的具体定义。不同于“例”环境，

### 性质（粉色） / 定理（蓝色） / 引理（蓝色） / 推论（紫色）及其结构化变体（结构性质、结构定理、结构引理、结构推论）

此四者本质上均为定理，仅有语义上的不同：

- 性质：总是跟随在一个定义之后，描述该定义所定义的数学对象具有的性质，通常较为简单，且描述了该对象定义的动机或等价定义。
- 定理：主要的数学成果，通常强大泛用。
- 引理：为主要定理证明服务的辅助性定理。
- 推论：主要定理的特殊情况或具体应用。

上述四类最终应当给出证明，但考虑到其工作量庞大不要求一次性完成。

证明书写要求将在后续给出

### 例（紫色）/ 反例（红色）

对于例和反例，不要求进行严格叙述，只需以自然语言描述即可。

### 注（橙色）

完全自由的自然语言表达，用于表达任何 CNL 无法表达的数学信息。

---

## 四、证明书写规范

证明书写的宗旨是避免线性叙事，不得让读者陷入琐碎的细节中；

应当先搭建证明框架，再逐步填充细节，保证读者时刻清楚当前证明的目标以及其如何服务于全局结论。

具体的书写规则如下：

1. 在证明目标改变，或者分支出子的证明目标时，用 `#目标` 函数指明证明目标，其设计细节参见模板文件。`name` 参数可指定证明目标的名称，`c` 参数可将“需证”改写为“需构造”，主要用于存在量词命题的对象构造。

2. 紧接着目标之下，用 `#证明` 或 `#构造` 函数来开启一个子证明或子构造环境。如果当前证明目标已经较为直观，则用一句简短的话语描述如何达成该目标，例如所套用的既成定理名称或者某种惯用证明方法；否则在子环境内进一步拆解目标，如此递归嵌套，直到目标较为直观为止。

因为本科数学定理一般较为简单，一般要求子目标的拆解次数不超过三层，如果超过三层则应反思知识组织架构是否合理，应当询问用户。

---

## 五、语言习惯

### 1. 类型论优先

- 用 `$x : T$` 表示对象类型，避免直接视类型为集合。
- 集合统一视为类型集合，使用 `SetTheory/export.typ` 中声明的常量，而非 ZFC 集合。
- 使用 `#Type-decl($a,b$, $T$)` 或 `令($x$, type: $T$, value)` 表示声明

### 2. 记号与链接

- 对于专业术语，引用本笔记或他笔记定义：用 `#概念名`（来自 export 的 optionLink）；
- 如果只是临时引用：`optionLink("UuidOrLabel", [显示名], url: "维基链接")`。

---

## 六、CNL 书写细则

书写正文时，必须调用 Fulcrum.typ 中已封装的 CNL 结构，而非在正文中手写对应词汇。

### 6.1 设、记作等封装

- **设**：通过条目的 `hypotheses` 参数传入，由 `ClauseHypotheses` 自动生成「设」。不得在正文中手写「设」。
- **记作**：通过 `定义` 的 `notation` 参数传入，由 `ClauseNotation` 自动生成「记作」。不得在 `value` 中重复写「记为」。

- **令**：使用 `令(varName, type: $T$, value: [内容])` 表达「令 … 为 …」。`value` 为必填时可省略 `type`。
- **句号**：条目的 `value`、`conclusion` 末尾不要加句号，模板会自动添加。

### 6.2 定义的 target 与 value

- **target（被定义陈述）**：写被定义对象的名称或描述，**不出现记号**。例如写「$V_1$ 与 $V_2$ 的交」，而非「交 $V_1 inter V_2$」。
- **value（定义内容）**：直接写对象的构造，**不重复被定义对象的表达式**。例如写 `${v | v in V_1, v in V_2}$`，而非 `$V_1 inter V_2 = {v | ...}$`。
- **notation**：记号统一在 `notation` 参数中给出，由模板生成「记作 …」。

### 6.3 前提与结论分离

- 以「对于任意」「若 … 则」等开头的前提，应移入 `hypotheses`，结论放入 `conclusion`，避免冗长符号堆积在正文中。
- 性质、定理的 `conclusion` 为**位置参数**（第 3 个），应写成 `#性质("标题", "", [结论], hypotheses: (...))`，勿用 `conclusion: [...]`。

### 6.4 尽量减少自然语言

- 除少数助词外，尽量使用 CNL 命令：`#iff`、`#implies`、`#线性空间`、`#域` 等。
- 未在本笔记定义的概念（如「域」），应到相关领域（如 `08-BasicAlgebra/export.typ`）查阅并引用，或先在对应 `export.typ` 中声明。

### 6.5 集合与「向量组」

- 「向量组」即向量的集合，写 `$A : Set(V)$`（或正文中 `$A : #Set($V$)$`），**不写**「向量组 包含于 $V$」（即不用 `$A subset.eq V$`）。不必单独定义「向量组」。

### 6.6 概念索引与标题

- 已在 `export.typ` 中定义的概念（如「基」），在**正文内容**中应使用带索引的版本（如 `#基`），便于链接。
- **章节标题**（`=`、`==`）和**条目标题**（定义、性质、定理等的 `title_cn`、`title_en`）中**不写索引**，保持标题简洁。

### 6.7 hypotheses 排版

- 若 `hypotheses` 中**第一条**以公式（`$`）或英文开头，应在 `[` 后加一空格，避免与「设」粘在一起，例如：`([ $K$ 是#域],)` 而非 `([$K$ 是#域],)`。
- **其余条**因接在逗号后，可不加空格，例如：`([ $K$ 是#域], [$V$ 是...], [$A : Set(V)$])`。

### 6.8 长公式与 display 模式

- 含巨算符（`sum`、`forall`、`exists`）或较长的公式，应使用 display 模式：在 Typst 中为 `$ 公式 $`（两侧留空）。
- 为相关条目设置 `bstyle: "display"`、`hstyle: "display"` 或 `cstyle: "display"`（视参数含义而定），详见 `Fulcrum.typ`。

### 6.9 合取命题与 structProp

- 合取命题若各分支有命名（如「(1) 线性无关；(2) 张成」），应使用 `structProp`（来自 `03-TypeTheory/export.typ`）：
  ```typ
  $ #structProp(
    (name: [线性无关], value: [$A$ #线性无关]),
    (name: [张成], value: [$forall alpha in S$，...])
  ) $
  ```

### 6.10 多分量的性质与 enum

- 性质、定理的结论若包含**多个并列分量**（如「(1) A 线性相关 iff …；(2) A 线性无关 iff …」），应使用 `#enum` 呈现有序列表：
  ```typ
  [#enum(
    [$A$ #线性相关 #iff ...],
    [$A$ #线性无关 #iff ...],
  )]
  ```
- `#enum(...)` 中 `+` 连接多个块会自动渲染为有序列表（enumerate）。

### 6.11 多命题等价与 令

- 若定理陈述**多个命题彼此等价**（如直和定理中四条件等价），参考 `26-RealAnalysis` 中「Lebesgue 可测函数的等价定义」的写法：
  - 用 `令(name, type: $Prop$, [命题内容])` 为每个命题命名；
  - 将这些 `令` 放入 `hypotheses`，并设 `hstyle: "display"`；
  - 结论写为 `[$name1 iff name2 iff name3 iff ...$]`。
  ```typ
  hypotheses: (
    令($alpha$, type: $Prop$, [命题1]),
    令($beta$, type: $Prop$, [命题2]),
    ...
  ),
  hstyle: "display",
  cstyle: "display",
  [$alpha iff beta iff gamma iff ...$]
  ```

---

## 七、符号与排版规范

书写公式时须遵循项目根目录下的 **符号习惯.md**。要点如下。

### 7.1 向量与矩阵

- 向量、矩阵使用**粗体**：在 Typst 中用 `bold(alpha)`、`bold(v)` 等，勿用 `mathbf`（LaTeX 写法）。

### 7.2 域与类型

- 表示域的 $K$ 使用 Typst 双线字母：写作 `KK`（即 `$KK$` 渲染为 $\mathbb{K}$），无需额外命令。

### 7.3 其他符号

- 算子用罗马字体、类型用空心/粗体等，详见 `符号习惯.md`。

### 7.4 Typst 语法注意

- Typst 与 LaTeX 不同：直和用 `plus.o` 而非 `oplus`；不等号用 `<=` 等；具体语法见 [Typst 官方文档](https://typst.app/docs)。

### 7.5 内容环境中的命令终止

- 在内容环境（如 `#注[...]`、`[ ... ]`）内，若 `#命令` 后紧跟 CJK 等可续写字符，Typst 会将其合并为同一标识符（如 `#基时` → 变量「基时」而非「基」+「时」）。此时应使用**英文分号 `;`** 显式终止命令，例如：`#基;时`、`#基;可并成`。

### 7.6 算子与 show 规则

- 明显为算子的记号应保证**名字与显示一致**。
- 若算子为 Typst 内置（如 `dim`），在 Style 中用 `show math.dim: optionLink(...)` 绑上索引。
- 若非内置（如 `span`、`rank`），直接定义同名命令，如：`#let span = (x) => $#optionLink("Span", $op("span")$, url: "...")(#x)$`，正文用 `span(S)`（见下）。
- **公式内调用**：若命令本身输出公式内容（`$...$`），在公式环境 `$...$` 内使用时**不要加井号**（`span(S)` 而非 `#span(S)`），否则内部参数会进入代码环境而非公式环境。

---

## 八、智能体经验与提示

以下为书写实践中总结的经验，供后续智能体参考。

### 8.1 查阅文档

- **Typst 语法**：模型未必熟悉 Typst，书写公式或排版时应**更频繁地查阅** [Typst 官方文档](https://typst.app/docs)，避免沿用 LaTeX 习惯（如 `mathbf`、`oplus`、`leq`）。
- **Fulcrum 接口**：各条目（定义、性质、定理等）的参数顺序、默认值、`style` 含义，以 `Fulcrum-Template-Typst/Fulcrum.typ` 为准，书写前应核对函数签名。

### 8.2 参数与调用

- `性质`、`定理` 的结论是**位置参数**，勿用 `conclusion:` 关键字。
- `令` 的 `value` 为必填时不能省略。
- `实例` 的 body 不要用第四个参数 `[]`，应写为 `#例(uuid, title_cn, title_en)[ 内容 ]`。

### 8.3 批量替换顺序

- 做符号批量替换（如向量加粗）时，**先替换更长模式**（如 `alpha_i`）再替换短模式（如 `alpha`），避免产生 `bold(bold(alpha))` 等错误。

### 8.4 跨笔记引用

- 书写前检查 `08-BasicAlgebra/export.typ` 等已有 `export.typ`，避免重复定义（如 `#域` 已存在，直接 `#import` 使用）。

---

## 智能体工作流程

用户要求智能体书写数学笔记时，只能要求其执行下述目标之一，而不应同时执行多个目标：

### 初始化新数学领域笔记

用户指定一个数学领域，智能体查询 MSC 分类标准，并生成对应笔记的目录结构，包括文件目录、主文件 `main.typ` 和概念库 `export.typ`，并在 `Mathematics/export.typ` 中加入对应 `#import` 和 `#show` 命令。

其中的部分要求：
1. 生成 `main.typ` 的库导入、样式设置、标题、目录等头部代码；
2. 生成 `export.typ` 的库导入代码，定义相应的 Style 样式设置函数；

如果某个数学领域已经有部分内容，则执行该任务时，智能体检查已有的头部代码，并按格式补全。

### 规划笔记章节结构

用户与智能体对话，可能会提供一些参考的数学教材和自己的想法，智能体规划笔记的章节结构，与用户对话确认后，用 `=` 和 `==` 语法书写章节标题，写入 `main.typ`。

章节编排应当尽量综合考虑到：
1. 避免前置章节依赖后续章节的知识；
2. 顺应直觉和学科发展，避免毫无动机的概念引入；
3. 避免难度梯度过大。

这几个要求常常是矛盾的，因此要权衡取舍，询问用户。

### 在已经初始化的笔记中添加新条目，但不写内容

用户首先告诉智能体自己的贡献者签名（在 `contributors.typ` 中），智能体在其中检查确保有自身模型的签名，后续书写的条目应在 `contributors` 参数列表中添加贡献者签名和自己的模型签名。

用户告知智能体自己想要撰写的知识范围，智能体结合整体的笔记框架，根据数学知识，在 `main.typ` 中生成相应的条目代码框架。这些框架只填中英文标题，不写具体内容。（对于一些“无名性质”或名称特别长的对象，可以省去英文标题）

用户与智能体对话，确认条目框架是否合理，如果不合理，则智能体修改框架，直到用户满意为止。

### 完成条目内容

用户确认条目框架合理后，智能体可以填充条目内容。填充须严格遵循第六节（CNL 书写细则）、第七节（符号与排版规范）、第八节（智能体经验与提示）。对于必要的条目，添加“注”条目以解释定义、定理的动机，以及其他需要自然语言描述的细节。

此环节中，定理性的条目不写证明。

### 证明书写

智能体不能一次完成大量证明，因为每一个证明都必须逐步精炼写出。证明书写必须遵循证明书写规范。

对于非主要定理（性质、推论），因为它们通常非常简单，可以批量完成围绕某个定义的所有性质的证明。

对于主要的理论框架（引理、定理），每次对话只能书写一个证明，不断精炼，直到用户满意为止。